# meshes/

Placeholder directory. The minimal placeholder URDF
(`urdf/so101_placeholder.urdf.xacro`) uses primitive boxes/cylinders and needs
**no** mesh files.

Drop the official SO-ARM101 `.stl` / `.dae` files here when integrating the real
model — see `../urdf/FETCH_OFFICIAL_URDF.md`. They will be referenced as
`package://so101_description/meshes/<file>`.
